[package]
name = "detect"
edition.workspace = true
version.workspace = true

[dependencies]
parser = { path = "../parser" }
flow = { path = "../flow" }
serde.workspace = true
linfa = "0.7"
linfa-trees = "0.7"       # decision tree — good starting point, interpretable
linfa-logistic = "0.7"    # logistic regression — alternative starting point
ndarray = "0.15"          # feature vectors / linfa's array type
